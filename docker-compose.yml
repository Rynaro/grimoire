version: '3.8'

services:
  grimoire:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: grimoire
    stdin_open: true
    tty: true
    volumes:
      - ./notes:/app/notes
    environment:
      - TERM=xterm-256color
    command: grimoire
